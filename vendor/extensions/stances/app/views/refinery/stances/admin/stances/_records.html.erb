<%= render 'refinery/admin/search_header', :url => refinery.stances_admin_stances_path %>
<div class='pagination_container'>
  <% if @stances.any? %>
    <%= render 'stances' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>

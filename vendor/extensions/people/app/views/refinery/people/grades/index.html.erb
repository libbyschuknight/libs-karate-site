<% content_for :body do %>
  <ul id="grades">
    <% @grades.each do |grade| %>
      <li>
        <%= link_to grade, refinery.people_grade_path(grade) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
